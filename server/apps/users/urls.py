from django.urls import include, path

# from .views import GoogleLogin, SelfGoogleLogin

urlpatterns = [
    # path("dj-rest-auth/google/", GoogleLogin.as_view(), name="google_login"),
    # path("auth/google", SelfGoogleLogin.as_view()),
]
